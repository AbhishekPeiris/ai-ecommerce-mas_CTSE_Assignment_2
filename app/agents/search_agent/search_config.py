from __future__ import annotations

SEARCH_CONFIG = {
    "name": "Search Agent",
    "role": "Dataset retrieval specialist",
    "goal": "Find relevant products that match the query constraints",
    "allow_delegation": False,
    "verbose": True,
}